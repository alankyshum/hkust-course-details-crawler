# HKUST course details crawler

> Crawl HKUST course info by supplying a list of course Codes

# To Use
0. Run `yarn install`
1. Edit `./data/courseList.json'
2. run `yarn start`
3. Get your `./data/courseDetails.json`
